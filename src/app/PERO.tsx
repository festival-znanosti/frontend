import React from 'react'

const PERO = () => {
  function handleClick(e: any) {
    e.preventDefault()

    console.log('The link was clicked.')
  }

  return <div>PERO </div>
}

export default PERO
